import Constants from "expo-constants";
import { Platform } from "react-native";
import { BACKEND_URL } from "src/lib/constants/AppConstants";
import { useAuthStore } from "src/lib/stores/AuthStore";

export const setupAppApi = () => {
  void Constants;
  void Platform;
  void BACKEND_URL;
  void useAuthStore;
};
